/**
 * Centralised animation timing constants.
 * Keep these in sync to maintain consistent feel across UI and 3D animations.
 */

export const ANIMATION_TIMINGS = {
  movementDurationMs: 600,
  attackVfxDurationMs: 850,
  turnBannerDurationMs: 700,
  hitFlashDurationSeconds: 0.6,
} as const
