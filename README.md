# ThreeGame — Next.js + React Three Fiber

Minimal Next.js scaffold with `@react-three/fiber` + `@react-three/drei`.

Getting started:

1. Install dependencies

```bash
cd /Users/richardpickup/Documents/ThreeGame
npm install
```

2. Run dev server

```bash
npm run dev
```

Open `http://localhost:3000`.
